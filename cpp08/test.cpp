#include <algorithm>
#include <iostream>
class Solution {
public:
        std::vector<std::vector<std::string>> groupAnagrams(std::vector<std::string>& strs) 
        {
            std::sort(strs.begin(),strs.end());
            std::vector<std::vector<std::string>>  myArray;
            std::vector<std::string>::iterator help;
            std::string a::;
            a.length(a);
    }
};